name: CI

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: caioreix/testing-actions/.github/workflows/setup.yml
      - uses: caioreix/testing-actions/.github/workflows/lint.yml
      - uses: caioreix/testing-actions/.github/workflows/test.yml
      - run: go build -o ./bin/bot -v ./cmd...
